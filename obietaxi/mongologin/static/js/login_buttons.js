/*
 * login_buttons.js
 *
 * Works with login.html. Defines what the two form buttons do
 * 
 * Currently, UNUSED***
 */

$(document).ready( function() {
    // What we do when the "Login" button is pressed
    $("#login_button").click( function() {
	// default action
    } );

    // What we do when the "Sign in with Google" button 
    $("#google_button").click( function( event ) {
	event.preventDefault();
	
    } );
}
	
